# 图隐 Tuyin 更新日志

## v2.0
- 上传图片框（封面 / 秘密图 / 隐写图）改为固定 4:3 长条比例：不再随图片尺寸撑高，图片完整居中显示在 4:3 框条内，界面更规整
- 新增莫奈取色（Material You 动态壁纸取色）：Android 12+ 主色 / 强调色（按钮、进度条、导航选中遮罩、选中反馈、下拉文字）跟随系统壁纸主题，低版本自动回退默认蓝
- 背景保持瓷白调性，莫奈色仅作用于主题强调色，整体观感随壁纸和谐变化

## v1.9
- 核心算法改造为分块流式：嵌入 / 提取逐 8×8 块计算标尺、灰度与输出，消除原版整图 double/float 全图数组，内存峰值与图片尺寸解耦，大封面嵌入不再被整图大数组拖累而 OOM 闪退
- 与原版全图算法逐字节一致：同封面 + 同参数嵌入结果完全一致，提取结果与网页版、旧版双向兼容（已本地回环 + 对照验证）
- 2048 级大封面嵌入流畅完成，嵌入 / 提取耗时稳定

## v1.8
- 修复可用容量进度条 / 嵌入进度条「一整条蓝」不随进度变化：填充层改为按进度裁剪绘制，未嵌入时显示灰色轨道，嵌入后按占用率实时填充蓝色

## v1.7
- 新增「开始嵌入」处理进度条：大图嵌入耗时较长时，蓝色进度条 + 百分比实时反馈，不再干等
- 嵌入进度按像素标尺生成 → DCT 块嵌入 → 输出重建分阶段计权上报，进度真实平滑

## v1.6
- 修复大封面增强（4096 / 自定义）闪退：封面解码按目标长边采样（不再全尺寸驻留），缩放中间位图用后即回收，封面增强改在后台线程执行，自定义放大加 24M 像素护栏，嵌入 / 模拟 / 提取统一捕获内存不足并给出可操作提示
- 修复可用容量进度条不可见：补上真正的蓝色进度填充层，占用率直观显示
- 统一 APK 签名：内置固定发布 keystore（PKCS12），今后所有版本使用同一把签名，覆盖安装不再因签名变化失败；仓库配置正式签名 secrets 时优先使用正式签名

## v1.5
- 封面增强真生效：长边双向重采样（2048 / 2560 / 3072 / 4096 / 自定义），小封面会被放大，容量随之提升，恢复的隐藏图更清晰
- 封面增强选项对齐原版：不放大 / 长边缩放到 2048 / 2560 / 3072 / 4096 / 自定义（原生下拉展开）
- 可用容量卡补充「实际载荷 · 占用比例」说明，进度条直观显示容量占用率
- 提取页「原图尺寸（可选）」说明文案对齐原版：仅当空间标尺读取失败时用于手动恢复原始尺寸
- 修复 v1.4 构建失败根因：saveBitmap 重载歧义

## v1.4
- 可用容量随画质滑条 / 自定义参数 / 展开收起实时刷新
- 封面增强改为 Material 单选下拉并真实生效
- 原图尺寸（可选）只填一栏时提示
- 新增「通道模拟 · 验证鲁棒性」：缩放 → JPEG 重压缩 → 再提取

## v1.3
- 提取结果 / 隐写结果卡片高度完全跟随图片宽高比，不再裁切、不挤压
- 顶部渐隐、液态玻璃底部导航等 UI 打磨
- 隐藏滚动滑块

## v1.2
- 保存图片到相册（读写权限申请）
- 关于页致敬原作者 GitHub
- 顶部透明占位与渐隐层调整

## v1.1 / v1.0
- 图隐 App 首个原生版本：嵌入 / 提取 / 保存 / 底部悬浮导航
