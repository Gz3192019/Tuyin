# 图隐 Tuyin 更新日志

## v1.5
- 封面增强真生效：长边双向重采样（2048 / 2560 / 3072 / 4096 / 自定义），小封面会被放大，容量随之提升，恢复的隐藏图更清晰
- 封面增强选项对齐原版：不放大 / 长边缩放到 2048 / 2560 / 3072 / 4096 / 自定义（原生下拉展开）
- 可用容量卡补充「实际载荷 · 占用比例」说明，进度条直观显示容量占用率
- 提取页「原图尺寸（可选）」说明文案对齐原版：仅当空间标尺读取失败时用于手动恢复原始尺寸
- 修复 v1.4 构建失败根因：saveBitmap 重载歧义

## v1.4
- 可用容量随画质滑条 / 自定义参数 / 展开收起实时刷新
- 封面增强改为 Material 单选下拉并真实生效
- 原图尺寸（可选）只填一栏时提示
- 新增「通道模拟 · 验证鲁棒性」：缩放 → JPEG 重压缩 → 再提取

## v1.3
- 提取结果 / 隐写结果卡片高度完全跟随图片宽高比，不再裁切、不挤压
- 顶部渐隐、液态玻璃底部导航等 UI 打磨
- 隐藏滚动滑块

## v1.2
- 保存图片到相册（读写权限申请）
- 关于页致敬原作者 GitHub
- 顶部透明占位与渐隐层调整

## v1.1 / v1.0
- 图隐 App 首个原生版本：嵌入 / 提取 / 保存 / 底部悬浮导航
